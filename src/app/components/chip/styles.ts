import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    paddingHorizontal: 8,
    paddingVertical: 4,
    backgroundColor: '#ffa1ad',
    borderRadius: 8,
  },

  label: {
    fontSize: 12,
    fontWeight: 600,
    color: '#1e1e1e',
  },
});
